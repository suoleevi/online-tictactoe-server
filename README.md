Server for online tictactoe web application. Uses NodeJS and Socket.io.

Server can handle multiple ongoing games and clients connecting, disconnecting and reconnecting.

Use with the frontend client. 

Start server with npm start.
Start client with npm start.